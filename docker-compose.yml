version: '3'

services:
  nginx:
    container_name: gobble_nginx
    build: ./nginx
    links:
      - gobble:gobble
    ports:
      - 8080:3000
    depends_on:
      - gobble

  gobble:
    container_name: gobble_server
    build: .
    ports:
      - 8080
